---
interface Props {
	title?: string;
	description?: string;
	canonicalPath?: string;
	socialTitle?: string;
	socialDescription?: string;
	socialImage?: string;
}

const {
	title = "Luc Contracting",
	description = "Luc Contracting provides general contracting, remodeling, repairs, and project coordination.",
	canonicalPath = "/",
	socialTitle = title,
	socialDescription = description,
	socialImage,
} = Astro.props;

const canonicalUrl = new URL(canonicalPath, Astro.site ?? Astro.url).toString();
const socialImageUrl = socialImage ? new URL(socialImage, Astro.site ?? Astro.url).toString() : undefined;
---

<!doctype html>
<!--  ❤︎ Built with care by jake@serverboi.org for Luc Contracting. ❤︎  -->
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1" />
		<script is:inline>
			if ("scrollRestoration" in history) {
				history.scrollRestoration = "manual";
			}

			if (!window.location.hash) {
				window.addEventListener("pageshow", () => {
					window.scrollTo(0, 0);
				});
			}
		</script>

		<link rel="icon" type="image/png" sizes="512x512" href="/uploads/favicon/favicon-512x512.png" />

		<link rel="apple-touch-icon" sizes="180x180" href="/uploads/favicon/apple-touch-icon.png" />
		<link rel="manifest" href="/uploads/favicon/site.webmanifest" />
		<meta name="theme-color" content="#070807" />
		<meta name="generator" content={Astro.generator} />
		<meta name="description" content={description} />
		<link rel="canonical" href={canonicalUrl} />
		<meta property="og:type" content="website" />
		<meta property="og:title" content={socialTitle} />
		<meta property="og:description" content={socialDescription} />
		<meta property="og:url" content={canonicalUrl} />
		{socialImageUrl && <meta property="og:image" content={socialImageUrl} />}
		<meta name="twitter:card" content={socialImageUrl ? "summary_large_image" : "summary"} />
		<meta name="twitter:title" content={socialTitle} />
		<meta name="twitter:description" content={socialDescription} />
		{socialImageUrl && <meta name="twitter:image" content={socialImageUrl} />}
		<slot name="head" />
		<title>{title}</title>
	</head>
	<body>
		<slot />
	</body>
</html>

<style>
	html,
	body {
		margin: 0;
		width: 100%;
		min-height: 100%;
		overflow-x: hidden;
		scrollbar-gutter: stable;
		background:
			linear-gradient(160deg, rgba(217, 178, 86, 0.12), transparent 26%),
			radial-gradient(circle at 85% 8%, rgba(217, 178, 86, 0.11), transparent 28%),
			#070807;
		color: #f4efe4;
		font-family:
			Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI",
			sans-serif;
	}

	html {
		scroll-behavior: smooth;
		scroll-padding-top: 96px;
	}

	@media (max-width: 820px) {
		html {
			scroll-padding-top: 82px;
		}
	}

	* {
		box-sizing: border-box;
	}

	a {
		color: inherit;
	}
</style>
